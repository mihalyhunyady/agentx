package com.epam.agentx.game.controller.GameController

import org.springframework.stereotype.Component

@Component
class GameController {
    fun startGame(roomId: Int) {

    }

    fun getPort(roomId: Int): Int {
        return 0;
    }
}